# Journal du cerveau

> Une ligne par évolution du wiki. Le plus récent en haut. Tenu à jour à **chaque étape** qui touche l'application.

## 2026-09-18 — Lot H2 : mères ordonnées (ordre de fabrication), mère dans mère, sous-lots et sous-sous-lots, bouton « Demande de prix », journal pleine largeur
- [Backend](02-backend.md) — module pur `src/nomenclature_arbre.ts` (147 tests) : contrôles 409 `cycle_composants` / `profondeur_max` / `composant_utilise`, plan de production d'une affaire (un sous-lot par composant, `LOT-…-ZZ.RR`, récursif, idempotent), DA agrégées par référence, route `POST /api/production/lot/:id/sous-lots`, vigilance « sous-lots non terminés » (aucune porte nouvelle), avancement de l'arbre, racines seulement pour libération / BL / KPI, journal `diffComposants`. [Frontend](01-frontend.md) — éditeur de mère ordonné, bouton libellé « Demande de prix », journal pleine largeur, listes de lots en arbre, fiche lot et OF avec sous-lots, goulotte en ordre de fabrication. [DB & API REST](03-db-et-api-rest.md) — **migration 018** + `cloud-14-lots-sous-lots.sql` (**cloud : Studio EN LIGNE, après `cloud-13`**, additif). [Utilisation](07-utilisation.md) — parcours « Fabriquer un assemblage ». Revue : 22 constats, tous réels, corrigés (Suivi LOT partiellement). Vérifié : `tsc` 0, harnais 60/0/1, tests purs 147 + 220, e2e Docker (dont cloud simulé sans `cloud-14`), Playwright (jeux `-TEST-` supprimés et relus).
- Documentation : `docs/CHANGELOG.md` (arbitrages A1 → A9, scripts à jouer, limites), `technique/06-modules/{be,production,expeditions,qualite,commercial}.md`, `03`, `05`, `07-api-reference.md` (contrats « Lot H2 », 401 routes), runbook (`cloud-14`, mise en service, 8 incidents), manuels HTML BE / Production / Qualité / Expéditions / Commercial (+ `src/manuels_contenu.ts`), manuels Markdown, formulaires BE, parcours 02 / 04 / 06 / 08, fiches de poste BE / production / qualité / logistique ; captures Docker : éditeur de mère, sous-onglet Mères, vue Lots en arbre, fiche du lot d'une pièce mère.

## 2026-09-16 — Lot G : cadence usine par site, planning en heures ouvrées, chevauchement du même process, remise en goulotte, process OAS
- [Backend](02-backend.md) — `src/cadence.ts` / `cadence_db.ts` (trois états de lecture : lisible, tables absentes, panne), routes `/api/production/cadence…` (date d'effet, confirmation rétroactive, modèles conditionnels), présence refusée sur un créneau fermé ; planning en **heures ouvrées** (`tempsPlanning`, `src/planning_cadence.ts` : 409 `heure_fermee`, 503 si la cadence est en panne) ; `src/poste_oas.ts` : G3 même process sur un poste (409, course rattrapée après écriture), G4 remise en goulotte (409 `successeurs_a_deprogrammer`, propagation, reçus signalés, `remis_goulotte_le`), G2 badge « → OAS » et « Lots à venir » ; `recu_le`, soldage de nuit, RH › Temps selon la cadence. [Frontend](01-frontend.md) — volet « Cadence usine », `CAD` et quatre copies client (`==CAD-MOTEUR==`, `==CC-MOTEUR==`, `==POSTE-OAS==`, `==PLAN-CADENCE==`), axe du jour et hachures, icônes de débordement, fenêtre « Remettre en goulotte », badges. [DB & API REST](03-db-et-api-rest.md) — **migration 014** + `cloud-12-cadence-usine.sql` (**cloud : Studio EN LIGNE**, aperçu `begin/rollback` : modifie les process OAS). [Utilisation](07-utilisation.md) — parcours « Organiser l'atelier ». Revue : 18 signalements, 17 défauts, tous corrigés. Vérifié : `tsc` 0, harnais 60/0, routes 20, règles pures 150 + 141 + 187 + 215 (parité 3 946 / 9 452 / 4 630), e2e 51, Playwright 35 + 33, Docker réel 14 + 9 (jeux `-TEST-` supprimés et relus).
- Documentation : `docs/CHANGELOG.md` (réponses de l'utilisateur, modèles d'horaires, point Haut jeudi/vendredi, choix à valider, `cloud-12`), `technique/06-modules/{production,oas,rh,expeditions}.md` (reliquat en Conforme **tranché**), `03` / `03b`, `04-auth-rbac.md`, `05-conventions-code.md`, `07-api-reference.md` (contrats « Lot G », 399 routes), runbook (`cloud-12`, mise en service : vérifier la cadence de chaque site, 14 incidents), manuels HTML Production / OAS / RH / Expéditions / index (+ `src/manuels_contenu.ts`), manuels Markdown, formulaires (Changer la cadence, Modèles d'horaires, Remettre en goulotte, présence, Shift), parcours 04 / 05 / 11, fiches de poste production / OAS / RH ; captures Docker : volet Cadence usine, fenêtre de changement, planning avec heures fermées et badges, message préventif, menu de présence d'un vendredi, OAS « Lots à venir ».

## 2026-09-16 — Planning : calage après le réglage du BDT précédent corrigé · Stock : Gestion en 2ᵉ onglet
- [Frontend](01-frontend.md) — **Planning** : le chemin critique était juste ; une carte de la goulotte glissée était posée à l'heure du **pointeur** (`_grabDX = 0` dans `onPendDragStart`) et non à celle du bord gauche de la carte que l'on aligne (carte 327-350 px, 50 px/h : jusqu'à 3 h 30 trop tard, sans calage). **Stock** : « Gestion du stock » en 2ᵉ onglet (`TABS` et `STK_TABS` de `src/stock_service.tsx`, même ordre ; défaut `mes` et hashes inchangés).
- Documentation : `docs/CHANGELOG.md` (citation, cause), manuel Stock (`html/stock.html` renuméroté, `html/index.html`, `stock.md`), `technique/06-modules/stock.md` (« Ordre des onglets »), `scripts_doc/gen_module_fiches.mjs`, `src/manuels_contenu.ts` régénéré.

## 2026-09-15 — Lot F : PV quantitatif / qualitatif, reliquat, Stock › Rangement / Mise en stock
- [Backend](02-backend.md) — PV de réception v2 (quantité reçue par ligne, NC fournisseur par ligne, quarantaine par ligne qualitative, excédent → validation Direction `excedent_reception`, reliquat → BC `-R<n>` date à valider, `BL.qte` = reçu) ; **plus aucun crédit de stock au PV ni à la décision Qualité** : file `mises_en_stock` (`ajouterAMettreEnStock`, idempotente, repli `crediterLignesRepli` sans 013) ; routes `/api/stock/mise-en-stock/:id/{accepter,annuler}`, `/entree`, `/sortie`, `/ajustement`, `/types-objet`, `/zones`, `/:id/emplacement` (+ historique), écriture Stock revérifiée, transitions conditionnelles ; **porte matière au rangement** (`ouvrirPorteMatiere` stricte → `EtatPorteMatiere`) ; décision Qualité répartie par ligne (`lignesDeQuarantaine`, `repartirPartAcceptee`) ; `recalculerStatutBc` compte toutes les quarantaines d'un BL (`quarantaines_attendues`) ; le « à ranger » compte dans DA du manque, `generer-da`, réappro auto, `scan-alertes`, cockpit, `kpi.ts` (fill rate hors reliquats). Règles pures `src/mise_en_stock.ts`, `src/pv_reception.ts`. [Frontend](01-frontend.md) — 1ᵉʳ onglet Stock « Rangement / Mise en stock » (À ranger, Ajustement, Types & zones), fenêtre de rangement, Niveaux d'approvisionnement (type, emplacement, historique), Entrée / Sortie fonctionnelles, « Masquer les produits vides » ; PV avec tableau des quantités et reliquat ; « Livraison partielle » retirée ; répartition Qualité ; Accepter / Refuser un excédent ; badge reliquat ; `notifDurable`. [DB & API REST](03-db-et-api-rest.md) — **migration 013** + `cloud-11-mise-en-stock.sql` (**cloud : à jouer dans le Studio EN LIGNE**, après `cloud-6` et `cloud-10`). Vérifié : `tsc` 0, harnais 60/0, règles pures 79 + 41 + 15 + 16, e2e (faux PostgREST) 43 + 49 + 21 + 44, Playwright 30 + 12 + 26, **Docker réel** 31 + 103 + 47 (jeux `-TEST-` supprimés et relus).
- Documentation : `docs/CHANGELOG.md` (demande, réponses de l'utilisateur, choix à valider, `cloud-11`), `technique/06-modules/{stock,expeditions,achats,qualite,direction}.md`, `03-base-de-donnees.md` / `03b`, `04-auth-rbac.md`, `07-api-reference.md` (contrats « Lot F », référence régénérée : 395 routes), runbook (`cloud-11`, mise en service, 15 incidents), manuels HTML Stock (réécrit), Expéditions, Achats, Qualité, Direction, Production (+ `src/manuels_contenu.ts`), manuels Markdown, formulaires champ par champ, parcours 03 / 06 / 08 / 09 / 13, fiches de poste logistique / achats / qualité / direction ; captures Docker : Rangement (À ranger, Ajustement, Types & zones), fenêtre d'acceptation, Stock en temps réel, Gestion, Niveaux d'approvisionnement, Mouvements, PV conforme avec reliquat et non conforme avec excédent, réception, BC de reliquat, décision Qualité répartie, refus d'excédent.

## 2026-09-15 — Production : soldage débloqué, PV de non-conformité et demande d'achat signés à l'atelier
- [Backend](02-backend.md) — **signature d'atelier** : une route self-service est ouverte à tout compte connecté, le handler juge le salarié du matricule + PIN (droits recalculés comme au login, `sessionDeSalarie` + `peutEcrireService(…, 'production')`). `/solder` réécrite (`src/soldage_bdt.ts` : opérateur qui a reçu le bon **ou** écriture Production ; tout salarié actif ; **matrice de compétences non bloquante** — elle stocke l'id du process, l'ancien contrôle comparait le libellé et refusait 20 opérateurs sur 22 ; « Reçu » exigé, transition conditionnelle, soldeur tracé sans réécrire `operateur_id`) ; `/recu` accepte une personne qui écrit en Production (elle devient `operateur_id`) ; `demande-achat-operateur` réécrite et `POST /api/production/nc` nouvelle (`src/prod_da_nc.ts` : `signataireProduction` 400 → 503 → 401 → 403 → saisie, `rattacherNcProd` affaire/lot/BDT relus et vérifiés, `insererNumeroteAvecRepli` renumérotation 23505 + colonnes facultatives absentes, `dateAtelier` Europe/Paris) ; `operateur-contexte` **supprimée** ; `/api/production/non-conformites` **sortie du self-service** (écriture Qualité) ; `ncEstClose` reconnaît « Soldé » (cockpit Direction aligné) ; PATCH RH part de `autorisationsUnion(rôles)` ; `lireToutesLignesStricte` (pagination, plafond PostgREST silencieux). [Frontend](01-frontend.md) — `blocDaNcProduction` : boutons « Demande d'achat » + « PV de non-conformité » dans `#svc-hdr-bar`, fenêtres hors panneaux, formulaire complet dès le clic, erreurs par `champ`, `pfEsc`, envoi unique ; fenêtre de soldage (« Reçu par », qui peut solder, anti double clic). [Docker](04-docker.md) — `maj` suffixe le commit gravé de `-dirty` si le dossier de travail est modifié. [DB & API REST](03-db-et-api-rest.md) — aucune migration. Revue : 14 signalements, 12 défauts réels, 10 corrigés. Vérifié : 182 cas purs, 98 appels d’API sur Docker (+ pagination 5, base injoignable 2, cockpit Direction 4), Playwright 40, `tsc` 0, harnais 60/0 (jeux `-TEST-` supprimés et relus).
- Documentation : `docs/CHANGELOG.md` (causes, décisions, limites), `technique/06-modules/{production,qualite,achats,rh,direction}.md`, `04-auth-rbac.md` (« Signature d’atelier »), `07-api-reference.md` (contrats « Production (15/09/2026) », référence régénérée), runbook (10 incidents), `12-docker-installation.md` (`-dirty`), manuels HTML Production / Qualité / Achats (+ `src/manuels_contenu.ts`), manuels Markdown, formulaires DA et PV champ par champ, parcours 04 / 06, fiches de poste production / opérateur / qualité ; captures Docker : bandeau à deux boutons, formulaires DA et PV, fenêtre de soldage.

## 2026-09-15 — Jauge de découpe · reliquat de réception réellement attendu
- [Frontend](01-frontend.md) — `#splitModal` : jauge du temps de réalisation (segments par morceau, reste gris, dépassement orange, réglage fixe à part) + curseurs par morceau (aimant sur le total, clavier), « Répartir également », « Reste sur le dernier », confirmation si somme ≠ V ; contrat `/separer` inchangé. [Backend](02-backend.md) — `bcsView.attend_reliquat` (livraison partielle déclarée `bons_de_livraison.partiel` ou remplacement Qualité sur la dernière réception) ; `_bcRecu` s'y fie au lieu du seul reste à recevoir (régression lot D : BC réceptionnés revenus « à réceptionner ») ; `construireBlReception` écrit `partiel`.

## 2026-09-14 — Lot D Expéditions : réception fournisseur hors France, PV de contrôle ligne par ligne, contrôleur habilité, certificat matière
- [DB & API REST](03-db-et-api-rest.md) — **migration 012** + `cloud-10-reception-fournisseur-pv.sql` (**cloud : à jouer dans le Studio EN LIGNE**) : 7 colonnes sur `bons_de_livraison` (N° de commande et de BL fournisseur, `hors_france`, `poids_matiere_kg` ≥ 0, `num_nomenclature` douane, `code_ewx` 1|2, `mode_arrivee`), `bons_de_commande.certificat_matiere_requis`, 5 colonnes sur `pv_controle` (`detail` jsonb v1, `controleur_id`, `controleur_nom`, `saisi_par`, `certificat_matiere` non_requis|confirme|absent), index unique partiel `ux_pv_controle_reception_bl` (créé seulement sans doublons, sinon WARNING). Valeurs de `statut`/`decision` du PV inchangées (CHECK cloud). [Backend](02-backend.md) — règles pures `src/reception.ts` (quantité = reste à recevoir, livraison partielle, BC à plusieurs articles, validation hors France), `src/pv_reception.ts` (validation du PV, contrôleurs éligibles, détail, NC), `src/certificat_matiere.ts` (+ `_db.ts`) ; `receptionner` et `pv` **sortis du self-service** (ouverts à tout compte connecté) et revérifiés par **`peutEcrireService`** (`src/auth.ts`) ; réception : BC mis à jour **conditionnellement avant** le BL (anti double réception), relecture du BL sur erreur ambiguë (503 `incertain`), reprise d'un BC figé ; nouvelle route `POST /api/expeditions/bl/:id/reception-infos` (correction) ; PV : lignes relues côté serveur, contrôleur revérifié (liste des salariés actifs ayant l'écriture Expéditions, calculée comme au login), 409 `certificat_change`, NC renseignée (`detecteur` « Réception », description ligne par ligne, `nb_pieces`) ; entrée en stock **ligne par ligne** d'un BC à plusieurs articles (tout le BL partait sur la 1ʳᵉ ligne) ; `POST /api/achats/bc/:id/certificat-matiere` + mention sur le PDF du BC. [Frontend](01-frontend.md) — fenêtre de réception (N° fournisseur, bloc hors France, bandeau de quantité, livraison partielle, mode « Corriger »), fenêtre de PV (Conforme / Non conforme, en-tête et lignes du BC avec Oui / Non et observation, « Tout à Oui », certificat), boutons PV grisés pour un lecteur, contenu commercial du BC vidé pour qui n'a pas l'écriture, délégation d'événements ; Achats : case « Certificat matière requis » (DA → BC et BC direct), colonne « Certificat matière » (Exiger / Retirer, cadenas « PV conforme »), `bcw_qte` numérique. ⚠ **La Qualité ne signe plus les PV de réception** sans le jeton `ecrire:expeditions`. Revue adverse : 15 signalements, 12 défauts, tous corrigés. Vérifié : `tsc` 0, harnais 60/0, 167 + 124 + 54 + 54 contrôles purs / e2e Docker (dont pannes injectées), Playwright 16, app Docker authentifiée 21 (jeux `-TEST-` supprimés et relus).
- Documentation du lot D : `docs/CHANGELOG.md` (décisions, scripts à jouer, limites), `technique/06-modules/{expeditions,achats,qualite}.md`, `07-api-reference.md` (contrats receptionner / reception-infos / pv / certificat-matiere), `03` / `03b` (colonnes 012), `04-auth-rbac.md` (self-service, `peutEcrireService`), runbook (`cloud-10` dans le Studio en ligne, 8 incidents), manuels HTML Expéditions / Qualité / Achats (+ `src/manuels_contenu.ts`), manuels Markdown, formulaires champ par champ, parcours 03 / 06 / 08, fiches de poste logistique / qualité / achats (générateur `gen_fiches_poste.mjs`) ; captures refaites sur la stack Docker (réception hors France, correction, PV conforme, PV non conforme, BC avec certificat, onglets Réceptions, Fournisseurs et Bons de commande).

## 2026-09-14 — Lot C Production : présences, 4 créneaux, réglage / réalisation, découpe réversible, chemin critique
- [DB & API REST](03-db-et-api-rest.md) — **migration 010** + `cloud-8` : index uniques exigés par les upserts (présences, affectations, compétences, catalogue fournisseurs, objectifs KPI ; la base Docker/VM née de `schema.sql` ne les avait pas → 42P10, clic de présence perdu). **Migration 011** + `cloud-9` : `bons_de_travail.temps_reglage`, `duree_avant_decoupe`, `temps_machine_avant_decoupe` (puis `POST /api/production/bdt/reglage/reconstituer`). [Backend](02-backend.md) — module pur `src/presences.ts` ; routes présence GET / POST (créneau inconnu → 400) / DELETE relu ; `/api/production/conge/:id/valider` (opérateurs seulement, conditionnelle, **pas d'auto-validation**) et `/api/rh/conge/:id/valider` rendue conditionnelle ; effets d'un congé lus de façon ciblée, échecs dits dans `warning` ; `etapeTempsMin` alignée sur `etapeDecomp` et déplacée dans `shared.ts` ; `reglageBdtDepuisGamme`, `planDecoupeBdt` ; `/separer` (seule la réalisation se découpe, **un seul porteur du réglage par famille**, saisie sur la racine seulement, ≤ durée, page périmée `duree_lue`/`nb_membres`, réduction conditionnelle) ; `GET /bdt/:id/temps` ; `/recoller` **rejouable** (durée d'origine posée comme repère, mises à jour conditionnelles ; sans colonnes : morceaux retirés d'abord) ; moteur **`src/gamme.ts`** du chemin critique (`auPlusTot`, `controleEnchainement`, `violationsEnchainement`, `successeursEnViolation`, `heureEntiereChemin`, `PLAN_HEURE_DEFAUT = 6` : un BDT posé sans heure compte à 6 h) appliqué par `/affecter` (toute pose écrit une heure), `POST /bdts`, `/affecter-st` ; PATCH générique : `debut`/`date_prevue` refusés, `duree`/`temps_alloue` jamais sous le réglage. [Frontend](01-frontend.md) — Présence : menu de créneau (1 appel, file par case, retour arrière, Effacer = DELETE, semaine verrouillée tant que non lue, fusion protégée par `PRES_TOUCHE`/`PRES_FIN`, clavier) ; 4 créneaux Matin/Journée/Après-midi/Soirée partout (texte AA foncé) ; goulotte (mini-barre réglage | réalisation, « M2 · 2/3 », « au plus tôt », « Annuler la découpe » verrouillé pendant l'appel), barre du Gantt (segment de réglage dans le repère du temps, hachures sombres), info-bulle / réception / soldage, fenêtre de découpe (réglage verrouillé ou saisissable, proposition incertaine jamais pré-saisie, « Relire le réglage »), zone rouge au glisser, carte « Enchaînements à revoir », « Dispo le … » des BST ; `pushNotif` à identifiants uniques. Revue adverse : **28 défauts examinés, 27 corrigés** (dont 4 majeurs : BDT posé au clic invisible au chemin critique, réglage compté deux fois dans une famille, recollage non rejouable, doc) ; données `-TEST-` antérieures au lot laissées (hors périmètre). Vérifié : `tsc` 0, harnais 60/0, 170 cas chemin (parité 4 420 comparaisons), 128 présences, 81 réglage, e2e Docker B/G/RV/A/PANNE, 71 contrôles sur l'app Docker authentifiée (jeux `-TEST-` supprimés et relus).
- Documentation du lot C : [DB & API REST](03-db-et-api-rest.md) — piège « upsert sans index unique = 42P10 » ajouté aux conventions. Doc de reprise : `docs/CHANGELOG.md` (causes, décisions, scripts à jouer), `technique/06-modules/{production,rh,direction}.md` (algorithme de reconstitution, table des cas du chemin critique, décisions à trancher), `07-api-reference.md` (contrats + page `/production/service`), `03`/`03b` (colonnes 011, index 010), runbook (reconstitution du réglage pas à pas depuis la console du navigateur, après `cloud-9` ou `erp-docker.sh maj`), manuels Production / RH / Direction (+ `manuels_contenu.ts`), parcours Production ; captures refaites sur la stack Docker (planning BDT, chemin critique, planning BST avec « Dispo le … », présence, découpe, création de BDT).

## 2026-09-14 — Coût horaire : seul le process en porte un (machine), homme au coût chargé RH
- [DB & API REST](03-db-et-api-rest.md) — **migration 009** + `cloud-7-process-taux-horaire-machine.sql` (**cloud : à jouer dans le Studio EN LIGNE**) : `process_atelier.taux_horaire_machine` (€/h HT, `null` = à saisir, contrainte ≥ 0), recopie de départ depuis `machines.cout_h` **seulement à la création de la colonne** (rejouable sans ré-alimenter un taux vidé). `machines.cout_h`, `postes.taux_horaire_manuel`, `machines_opex.taux_horaire` laissés en base, plus lus (sauf transition) ni écrits. [Backend](02-backend.md) — moteur unique `src/shared.ts` : `typeProcess`, `construireTauxAtelier` (taux machine du process lu en direct ; taux homme = moyenne des `taux_horaire_charge` des opérateurs actifs du site ; transition = propriété absente → `cout_h`), `etapeDecomp` (ROP → homme, RGM → homme + machine, THV → homme, TMV → machine ; drapeaux `manquants`), `etapeLibreVersEtape`, `coutReelBdt` ; `computePosteRates` / `HEURES_JOURS_OUVRES` / `coutHoraireReel` **supprimés** ; `getTauxAtelier()` remonte l'erreur ; `/api/be/analyse-dt/:id` 503 si taux illisibles ; `PROC_COLS` + `taux_horaire_machine` (400 si invalide, réessai sans la colonne + `avertissement` cloud-7) ; routes machine n'écrivent plus `cout_h` ; `DELETE` machine détache sans convertir (503/400 vérifiés) ; etapes-libres 409 sans `vider:true` ; `recomputeCmdCout` écrit `null` si un coût manque ; `etapeTempsMin` compte le RGM. [Frontend](01-frontend.md) — Production : taux sur les modales process (sélecteur de type à l'édition), pastilles « X €/h » / « taux à saisir » / « coût RH », colonnes Taux/h / OPEX théorique / Taux/h effectif et bouton `posteSetTaux` retirés, `PROCESS`/`MACHINES_PROC` en `sjX` ; BE : `BE_ETAPE_COUT_JS` (même moteur injecté dans la nomenclature et `/be/analyse`), indications sous le process, encarts « coût incomplet », blocage si taux illisibles ; Finances (taux machine des process, imputations au coût réel, simulateur ROP/RGM/THV/TMV, boutons factices retirés) ; Maintenance (plus de taux ni de coût absorbé) ; Plans ; fiches 360 « — » + bandeau. Vérifié : `tsc` 0, harnais 60/0, 114 cas moteur, parité client/serveur 11 697 comparaisons, Playwright, bout en bout Docker (jeu `-TEST-` supprimé et relu).

## 2026-09-13 — Découpe d'un BDT : goulotte seule, temps libre
- [Frontend](01-frontend.md) — `splitModal` déplacée dans `#ppanel-gantt-bdt` (elle était sous l'onglet caché Dashboard Production : invisible depuis le planning) ; entrée « Séparer en morceaux » retirée du clic droit du Gantt ; fenêtre refaite (une ligne d'heures par morceau, ajout/retrait sans perte des saisies, total et écart informatifs, précision 1/100 h) ; `pendSort` numérique. [Backend](02-backend.md) — `/separer` : 409 si le BDT est posé (négation exacte de `enGoulotte`), `parts` obligatoire et validé, **plus de prorata**, `duree = temps_alloue = part`, morceaux avec process + poste mais **sans machine** (heures machine de Maintenance), lecture stricte (`getBDTStrict`, `idsMorceauxBDT`), retour arrière vérifié (`retirerBDTRows`). [DB & API REST](03-db-et-api-rest.md) — aucune migration ; `duree` du cloud = 2 décimales (vérifié). Vérifié jeu `-TEST-` (supprimé) : 70 contrôles API + écran.

## 2026-09-12 — Lot fournisseur non conforme : renvoi / dérogation / entrée partielle · avoirs fournisseurs · motif de suppression
- [Backend](02-backend.md) — `POST /api/qualite/quarantaine/:id/decision-fournisseur` : les trois issues d'un lot reçu non conforme, **réservation atomique** de la décision avant tout effet (stock, avoir, décrément du BC), effets idempotents, avertissements rendus plutôt qu'avalés ; `recalculerStatutBc` (règle unique : PV conforme **ou** lot tranché) partagé avec le PV ; `entrerStockReception(bc, bl, qteAcceptee?)` ; quarantaine **systématique** au PV non conforme d'une réception ; gardes sur `Statuer`, `/liberer`, `/rejeter` et la décision Direction (qui ne peut plus libérer un lot fournisseur sans son entrée en stock ni son avoir) ; routes `/api/achats/avoirs-fournisseurs*` (créer, reçu, imputer, annuler) à transitions conditionnelles ; motif obligatoire et tracé pour supprimer une nomenclature validée (refus si le journal manque). [DB & API REST](03-db-et-api-rest.md) — **migration 008** + `cloud-6` : colonnes de décision sur `quarantaines` (aucune valeur de `statut` nouvelle) et table **`avoirs_fournisseurs`** (pas de DELETE, index unique par quarantaine). [Frontend](01-frontend.md) — `appMotif` partagée, fenêtre « Décider » en Qualité, 7ᵉ onglet **Avoirs fournisseurs** aux Achats, carte **Retours fournisseurs** aux Expéditions, état du lot sous un PV non conforme. Revue adverse (28 agents) : **5 défauts confirmés, corrigés** — anciennes sorties laissées ouvertes tant que la base n'a pas la 008 (sinon lot sans issue et affaire bloquée), décision refusée sans quantité (sinon renvoi invisible et porte matière ouverte à tort), imputation interdite avant réception de l'avoir (sinon son n° ne peut plus être enregistré), porte matière qui se rouvre quand la matière est rachetée, fenêtres d'avoir qui se ferment au succès. Vérifié : **69 contrôles d'API bout-en-bout** sur Docker (jeu `-TEST-` supprimé), harnais 60/0, `tsc` 0 erreur.

## 2026-09-11 — Nomenclatures validées : journal EN 9100
- [DB & API REST](03-db-et-api-rest.md) — table **`nomenclature_journal`** en ajout seul (déclencheurs modification / effacement / vidage, heure imposée par la base, pas de FK) : **migration 006** + `cloud-5-nomenclature-journal.sql` ; **migration 007** : `id` généré par défaut sur les 50 tables Docker qui n'en avaient pas (créer une nomenclature échouait sur Docker / VM). [Backend](02-backend.md) — `auteurDe` (la session, jamais le corps ; compte de secours sans matricule) + `tracerNomenclature` / `suiviEN9100` (validée, ou déjà au journal) branchés sur POST / PUT (503 si l'état avant est illisible) / programmes / nouvel-indice / DELETE (journal AVANT l'effacement, effacement vérifié) / GED / références client ; `GET /api/nomenclature/:id/journal` ; écarts calculés par le module pur `src/nomenclature_journal.ts` (toutes les colonnes et clés d'étape, alignement LCS de la gamme). Import et miroir protégés. [Frontend](01-frontend.md) — carte « Journal des modifications · EN 9100 » ; avertissement orange sur `journal:false` dans tous les appelants. Deux revues adverses (10 puis 7 défauts confirmés, corrigés). [Docker](04-docker.md) — **le lanceur de migrations se connecte en `supabase_admin`** : connecté en `postgres`, il ne pouvait modifier aucune table d'une VM née de `schema.sql` (002-005 probablement jamais appliquées) ; contrôle des mots-clés : exception `before truncate on`, concaténation de littéraux refusée. Vérifié jeu `-TEST-` (supprimé) : 63 contrôles API + 8 avec authentification + 50 tests unitaires.

## 2026-09-11 — Planning : un BDT n'est sur le planning que s'il y a été posé
- [Frontend](01-frontend.md) — `enGoulotte(b)` (statut `a_programmer`, sans process ou **sans jour** ⇒ goulotte) remplace le critère `process==='pending'` dans Gantt, goulotte, stats et réception : un BDT à programmer n'est plus posé d'office à 6 h. Double-clic : programmé → **réception** (matricule + PIN), reçu → sortie matière. Bouton **ciseaux** sur les cartes de goulotte. `?focusBdt=` sur `/production/service`. [Backend](02-backend.md) — créateurs de BDT en `a_programmer` ; `/affecter` (jour toujours écrit, 409 si reçu/soldé, machine suit le process, repli heure pleine si `debut` encore entier) ; `/deprogrammer` (retour complet, 409) ; `/recu` (409 hors planning, heure de Paris) ; `/separer` (morceaux d'abord + retour arrière `supprimerBDTRow`, en goulotte, suffixe depuis la racine) ; `gamme.ts` (BDT reçu ≠ soldé, tous les morceaux d'une étape) ; `/production/gantt-bdt` redirige. [DB & API REST](03-db-et-api-rest.md) — **migration 005** `bons_de_travail.debut` → numeric (et `cloud-4-bdt-debut-numerique.sql`). Vérifié jeu `-TEST-` (supprimé) + navigateur.

## 2026-09-11 — Réception : le stock attend le PV conforme
- [Backend](02-backend.md) — crédit de stock déplacé de `/receptionner` vers le **PV conforme** (`entrerStockReception`, quantité du BL, anti-doublon par requête ciblée `mouvementEntreeExiste` — la liste des 200 derniers mouvements ne voyait pas les anciens) ; **un PV par réception** (409) ; le PV vise le BL de la ligne cliquée ; le BC n'est plus écrasé par `recu` et passe `controle` quand toutes ses réceptions ont un PV conforme ; porte matière (`ouvrirPorteMatiere`) ouverte au PV conforme, plus à la réception. [Frontend](01-frontend.md) — onglet Réceptions : état par BL (PV à faire / Conforme / Non conforme), quantité décimale, messages corrigés. [DB & API REST](03-db-et-api-rest.md) — aucune migration. Vérifié sur jeu `-TEST-` (supprimé).

## 2026-09-11 — Nomenclatures : une fiche validée reste validée, le plan s'ouvre
- [Frontend](01-frontend.md) — `nomSave` : une nomenclature **déjà validée reste `valide`** à l'enregistrement (les deux boutons « Enregistrer » envoyaient `en_cours` et la dévalidaient en silence : elle quittait la liste des faites ET la cascade de production). Lien **« Ouvrir le plan »** sous le champ *Nom du fichier CAO* (`nomRenderLienPlan`) — le nom était du texte inerte, le fichier dans la zone GED plus bas. [Backend](02-backend.md) — **garde-fou serveur** dans `PUT /api/nomenclature/:id` : un statut ≠ `valide` reçu sur une fiche `valide` est ramené à `valide` (vieux onglets), sauf `{ devalider: true }` explicite ; lecture en échec ⇒ statut non touché. `GET /api/ged/file/:id` inchangé : il servait déjà le fichier (vérifié authentifié). [DB & API REST](03-db-et-api-rest.md) — aucune migration. Reproduit AVANT correction sur des fiches `-TEST-` (supprimées). Harnais 60/0.

## 2026-09-10 — Programmer n'est pas produire : planning ouvert, porte au départ du BST
- [Frontend](01-frontend.md) — Production : la carte au-dessus des Gantt ne dit plus « ordres retenus hors du planning » mais **« programmables, mais pas encore lançables »** — les BDT y sont TOUS, avec ce qu'ils attendent. Expéditions : dans le Calendrier, le bouton « Traiter » d'un départ ST cède la place à une **pastille verrou portant la raison** ; dans Envois › Sous-traitance, la colonne Statut affiche « attend `<n° opération>` ». [Backend](02-backend.md) — `filtrerBdtsPrets()` **supprimée** (plus aucun BDT n'est retiré du planning) ; `bdtBlocage()` → **`bdtVigilance()`**, même calcul, valeur d'avertissement. Nouveau module **`src/gamme.ts`** (pur) : gamme = fusion BDT+BDS par `seq`, `bdsEnvoiBlocage()`, annulées enjambées. Refus **serveur 409** sur `POST /api/expeditions/bds/:id/envoyer` ET sur le `PATCH` générique passant `statut: 'envoye'`. ⚠ **Trou RBAC corrigé** : le bouton d'envoi/retour ST des Expéditions appelait `/api/production/…` → évalué sur le service `production`, où `logistique` n'a que la lecture → **mort en silence pour ceux dont c'est le métier** ; routes `/api/expeditions/bds/:id/{envoyer,retour}` ajoutées. [DB & API REST](03-db-et-api-rest.md) — **aucune migration**. Vérifié sur la base Docker (2 BST `-TEST-`, supprimés et suppression relue) : 4 cas de porte confirmés. Harnais 60/0.

## 2026-08-04 — Lot prod/compta : DUER strict, planning, numérotation LOT/BDT, nav lots→planning, EDF
- [Frontend](01-frontend.md) — DUER : menu limité aux 20 familles ED 840 (retrait 21-25). Planning ancien (`pageGanttBDT`) : goulotte au-dessus, **poste sur les cartes BDT**, filtre OAS réparé. Commandes Production : n° `CMD-YYYY-XXXX` en premier. Liste des lots : n° BDT cliquable → planning (`?focusBdt`, surligne + cale la journée), colonne « Programmé ». Compta : sous-onglet **Énergie / EDF** (échéancier + KPIs report). [Backend](02-backend.md) — **Numérotation déterministe** `LOT-YYYY-XXXX-ZZ` / `BDT-YYYY-XXXX-ZZ-AA` (rang stable pièce/étape, idempotent) sur les 2 chemins (`cascadeLotsBdtBst` + `generer-bdt`) ; `est_oas` transmis au front (route planning) ; `genEcritureAchat` écrit le code de compte nu. [DB & API REST](03-db-et-api-rest.md) — **migration ids existants** appliquée cloud (anon ; tables RLS `mouvements_stock`… vides) + Docker ; EDF **sans nouvelle table** (factures fournisseur 606300, reprises en trésorerie) ; compte 606 ajouté à la Balance ; `factures_fournisseur` versionnée + créée dans Docker. Gotcha : `lots` sans colonne `num_affaire` (lien via `cmd_id`). Harnais 61/0, déployé Cloudflare + Docker, les 2 bases migrées.

## 2026-08-03 — Audits : traitement de surface scindé OAS + Surtec 650 (poste-flash)
- [Backend](02-backend.md) / [DB & API REST](03-db-et-api-rest.md) — rappel structure : **T1..T9 = thèmes**, pas postes. Le traitement de surface = 2 procédés internes ⇒ l'unique ligne poste-flash devient **`PRG-2026-T10` « Traitement de surface — OAS (anodisation) »** + nouvelle **`PRG-2026-T16` « Traitement de surface — Surtec 650 (chromatation trivalente) »** (type `poste`, `GRL-FLASH`, T1..T6/T9). Appliqué au **cloud** (rename PATCH + insert POST via l'API programme, RLS anon) et au seed `scripts_import/audit_seed.py`. [Frontend](01-frontend.md) — exemples de saisie UI précisés (`src/qualite.tsx`). Harnais 61/0.

## 2026-07-31 — SEIRICH Phase 4 : cartographie du risque chimique (Plan / Bâtiment)
- [Frontend](01-frontend.md) — bouton **« Vue risque chimique »** sur la maquette (`src/plans.tsx`) : colore les repères chimie/ATEX par niveau SEIRICH (vert/orange/rouge) + légende par niveau + deep-link repère chimie → fiche 360 `/securite/chimique/:id`. [Backend](02-backend.md) — niveau précalculé serveur (mapping CHIMIE `computeRisqueChimique`+expositions, ATEX via `type_zone`) ; couleur au rendu (`SEIRICH_NIVEAUX` injecté, non persistée) ; route `/plans/service` +`getHseExpositions`. Aucune migration. Vérifié SSR+e2e cloud (86 produits cotés, 0 erreur). **➡ SEIRICH complet (4 phases + fiche 360).**

## 2026-07-31 — Lot QSE (4 chantiers) : chimie/EPI, dérogations, quarantaine, DUERP ED 840
- [Frontend](01-frontend.md) — **(1)** Chimie : N° d'identification, quantité+unité en liste, bouton « Pictogrammes » (matrice de compatibilité de stockage CLP/SGH), bouton « + SEIRICH » par produit, EPI sans colonne Type. **(2)** Dérogations en circuit « offre » : statut *envoyée*, bouton « Traiter le refus » → avoir/commande P. **(3)** Quarantaine : bouton « Mettre en quarantaine » sur NC (objet libre) + bouton « Statuer » 3 branches (validation Direction / dérogation / décision immédiate). **(4)** DUERP : familles recalées INRS ED 840. [Backend](02-backend.md) — routes `POST /api/qualite/derogation/:id/traiter-refus`, `/api/qualite/nc/:id/quarantaine`, `/api/qualite/quarantaine/:id/statuer` (+ hook décision Direction) ; réutilisent createCredit/createCommandePrioritaire/createQuarantaine/createValidation/createDerogation. **Aucune migration** pour dérogations & quarantaine (statuts texte, colonnes `nc_id`/`nc_ref`/`decision_client` existantes). [DB & API REST](03-db-et-api-rest.md) — 2 migrations optionnelles fournies : `chimie_num_identification.sql` (N° identif), `duer_familles_ed840.sql` (6 libellés DUER). Revues adverses → 3 correctifs (idempotence dérogation sur decision_client ; verrou quarantaine en_validation + refus révision→en_cours ; vérif erreurs). Harnais 61/0, e2e OK, déployé par lot.

## 2026-07-31 — Fiche produit chimique 360 (risque + FDS + péremption + stock)
- [Frontend](01-frontend.md) — nouvelle page `/securite/chimique/:id` (`src/chimique_fiche.tsx`) ouverte via icône ⧉ dans Sécurité › Chimie : 6 cartes (danger/étiquetage + SEIRICH 3 axes, situations d'exposition, FDS, péremption, stock, actions) + PDF de marque. [Backend](02-backend.md) — agrégateur `getHseChimiqueDetail(id)` (queries.ts) à **jointures souples sans migration** : stock (`getStockReel`, ref_stock multi-codes ↔ reference), périssables (code GPAO ou nom normalisé), actions (source_type exposition). Route GET, RBAC segment `securite` déjà mappé. Boucle les 3 décisions SEIRICH (fiche unifiée). Rendu SSR + e2e cloud OK, harnais 61/0.

## 2026-07-31 — SEIRICH Phase 3 : axes Incendie+Env, plan d'actions, rapport PDF, export
- [Backend](02-backend.md) — `computeExpositionIncendie`+`computeExpositionEnv` (`src/qref.ts`, ND2233/R409, danger-gated : incendie +overlay **ATEX**/comburant ; env modulé par rejet) ; route `GET /api/export/seirich.xlsx` (buildXlsx 3 feuilles, gardée service `securite`). [Frontend](01-frontend.md) — sous-onglet Exposition passe à **3 axes** (Santé/Incendie/Env/Retenu + badge ATEX + matrice max) ; bouton **« → Action »** (`secACFromExpo`→action corrective via SEC_PREFILL) ; **rapport PDF** de marque `risqueChimiquePdf` (3 sections) ; bouton **Export SEIRICH**. [DB & API REST](03-db-et-api-rest.md) — recherche du **format d'import INRS** : SEIRICH n'importe que des classeurs Excel calqués sur son modèle vierge (Produit/Substance/Composition/Conf), export inventaire NON ré-importable → ERP produit un tableur à recopier. Moteurs unit-testés, rendu SSR + e2e cloud (2 expositions réelles) OK. ⚠ gotcha : import qref manquant passe build+harnais (données simulées 0 exposition). Build, harnais 60/0.

## 2026-07-31 — SEIRICH Phase 2 : exposition par situation de travail
- [Frontend](01-frontend.md) — nouveau sous-onglet **Sécurité › Chimie › Exposition par situation de travail** : KPIs + liste éditable (Potentiel · Résiduel · Retenu cotés en direct) + **matrice produits × unités de travail**. [Backend](02-backend.md) — moteur `computeExpositionSante` (`src/qref.ts`, méthode **INRS ND 2233 / R 409** vérifiée sur le PDF INRS : 2 scores distincts potentiel/résiduel-Sinh, plancher CMR, EPI informatif, Qmax de zone auto) ; table **fail-soft** `hse_exposition_chimique` (`scripts_import/exposition_chimique_schema.sql`, à exécuter) ; CRUD via `/api/securite/*`, cotation serveur `_coteExposition` + recalcul live à l'affichage. Décisions user : niveau **intermédiaire**, ERP **+ export SEIRICH** (Phase 3), **fiche produit unique** (à venir). Moteur unit-testé (3 ex. R409 + CMR), rendu SSR + e2e OK. Build, harnais 60/0.

## 2026-07-31 — Nouveau service ENVIRONNEMENT (regroupement)
- [Frontend](01-frontend.md) — nouveau service **Environnement** dans la sidebar (sous Sécurité, `src/environnement.tsx`, route `/environnement/service`) qui **regroupe tout l'environnement** : Déchets (BSDD/Trackdéchets), Rejets & mesures (eau/air/bruit/énergie + relevés OAS), ATEX, **Aspects & impacts (ISO 14001 §6.1.2)**, RSE, Conformité env. (ICPE/14001), Tableau de bord (dont axe env SEIRICH + lien audit thème J). Ces onglets sont **retirés de Sécurité** ; la conformité de Sécurité ne montre plus que les domaines SST. [Backend](02-backend.md) — page **autonome** (helpers + script CRUD propres) qui **réutilise l'API `/api/securite/*`** et les tables `hse_*` (aucune migration pour l'existant) ; nouvelle entité `aspects-impacts` (table fail-soft `hse_aspects_impacts`, schéma `scripts_import/env_aspects_schema.sql`, criticité + AES calculés serveur). **RBAC** : service `environnement` calqué sur `securite` (`src/auth.ts` ROLE_MATRIX/MENU_SERVICES/PAGE_SEG_SERVICE + sidebar `src/shared.ts` + route/carte/CRUD `src/index.tsx`). Vérifié e2e : 7 onglets, bascule, modale « Nouvel aspect », CRUD bout-en-bout (`-TEST-` déchet créé+supprimé), Sécurité intact. Build OK, **harnais 60/0**.

## 2026-07-31 — Préparation audit externe : hygiène du dépôt + doc
- [Maintenance](06-maintenance.md) — dépôt prêt pour un **audit préliminaire de maintenabilité** externe : vraies données fournisseurs **retirées du dépôt** (gitignore), inventaire des **variables d'environnement** (`docs/technique/10`), `SECURITY.md`, **guide d'audit** (`docs/technique/11`), références générées (03b tables / 07 API) régénérées. Secrets prod `JWT_SECRET`/`BOOTSTRAP_PIN` à poser en secrets Cloudflare ; clé anon à roter après l'audit. `AUTH_ENFORCE=on` confirmé en prod.

## 2026-07-30/31 — Module « Audits de conformité » (SMI QSE + SI)
- [Frontend](01-frontend.md) — nouvel onglet Qualité › **Audit Conformité** (sous-onglets Programme [ordonnancement annuel + reconduction N+1 + catégories process/poste/procédé], Grilles, Constats, Référentiels) ; **fiche d'évaluation** remplissable (Enregistrer / **Solder** → rapport PDF) ; **Conformité vive** (auto-observation) + matrice de couverture déplacées dans le **Dashboard Qualité**. [Backend](02-backend.md) — tables `audit_programme`/`audit_grille`/`audit_question`/`fai` (`scripts_import/audit_schema.sql`), **moteur de sondes** `getAuditAutoStatus()` (ECME/certifs/DUER/FDS/VGP/bains/NC…), routes `/api/audit/*` + PDF, reconduction annuelle. [DB & API REST](03-db-et-api-rest.md) — **questions VERBATIM du classeur Excel** (58 + 9 flash = 67), source `scripts_import/audit_questions.json`. Intègre **ISO 45001 + 27001** (thèmes A–K, T1–T9). Vérifié e2e Docker + cloud ; déployé.
- Autres évolutions de fin juillet : **exports Excel** fournisseurs/clients (`src/xlsx.ts`, fflate) ; **plan de contrôle PRP1-D9** (19 lignes) ; **libération des lots** (contrôle final) ; **nomenclature accessoires** (prix catalogue ou demande de prix, plus de saisie manuelle) ; **postes glissables** + machine en face du process ; **planning** BDT à classer au-dessus (pleine largeur).

## 2026-07-22 — Commercial : devis PDF (lot offres de prix, 1/n)
- [Frontend](01-frontend.md) — bouton **« Devis PDF »** sur les offres envoyées → devis générique (logo, client + TVA intra + contact, pièces réf/désignation/qté/**PU vente**/total, transport si Franco, HT/TVA/TTC, mode de règlement, CGV) via fenêtre d'impression (pattern fdsPdf). [Backend](02-backend.md) — `GET /api/offre/:id/devis` (offre + client + pieces_detail.prix_vente de la DT). Migration `commercial_offres_schema.sql` (TVA intra, contacts, mode/frais livraison, date_envoi, avoirs mode_restitution) écrite — champs tolérés absents avant application. Vérifié : build ; harnais 59/0 ; e2e (PROMISTEL TTC 4191,84) ; navigateur 0 erreur. ⏭️ suite lot offres après migration (règlement/livraison/à relancer/fiche client/avoirs).

## 2026-07-21 — Direction : traçabilité jalons traités + consulter l'objet soumis
- [Frontend](01-frontend.md) — cockpit Direction : bouton **« Traiter »** par jalon critique (modale note) + section **« Historique des jalons critiques traités »** (traçabilité, persiste après résolution) + pastille « ✓ Traité » ; bouton **œil** dans « À valider » et « Traités » ouvrant une modale des **champs réels de l'objet** (commande/facture/NC/avoir/salarié/BC…) + deep-link « Ouvrir dans le service ». [Backend](02-backend.md) — `POST /api/direction/jalon-traite` (ligne `validations` `statut='traite'`, idempotente sur `ref_table|ref_id`, **sans migration**) + `GET /api/direction/source` (lecture générique whitelistée via `getSourceRow`) ; route `/direction/service` charge `jalonsTraites` (distinct des décidées). Vérifié : build ; harnais 59/0 ; e2e réel 13/13 ; navigateur (45 boutons Traiter, modales OK, deep-link `/commercial/affaire/…`, 0 erreur console) ; déployé.

## 2026-07-21 — Analyse DT en 2 volets (Seem/Semrac) → offre fusionnée
- [Frontend](01-frontend.md) — formulaire `/be/analyse` : quand une DT mêle des pièces **Seem** et **Semrac**, barre **« Analyse en 2 volets »** (onglets + badges terminé/à faire) ; on n'affiche/chiffre **qu'un site à la fois**, bouton **« Terminer le volet SEEM/SEMRAC »**. Mono-site = inchangé. [Backend](02-backend.md) — `POST /api/dt/:id/analyse` accepte `site` ; avancement par volet dans la méta `__analyse_be__` (clé `sites`, **sans migration**) ; **l'offre fusionnée n'est créée qu'aux 2 volets terminés** (`statut='dans_offres'`), montant/coût **recalculés sur `pieces_detail` entier** (chaque volet ne renvoie que ses pièces). Tant qu'un volet reste : DT analysable, `partial:true`, pas d'offre. Site d'une pièce = `pieces_detail[].activite`. Vérifié : build ; harnais 59/0 (⚠ `index.tsx` hors périmètre harnais → sûreté script client prouvée en navigateur, 0 erreur) ; **e2e réel 18/18** (partial→offre fusionnée 162/135 ; régression mono-site) ; déployé (git + wrangler).

## 2026-07-21 — Validation Direction : refus « agissant » (annulation totale / révision)
- [Frontend](01-frontend.md) — le bouton Refuser (cockpit Direction) ouvre une **modale** : motif + case **« Annulation totale »** (cochée = annulation ; décochée par défaut = **renvoi en révision**). Badge d'origine « Direction ✗ Annulé » / « Direction ✎ Révision » ; Traités « REFUSÉ · ANNULATION/RÉVISION ». [Backend](02-backend.md) — route `/decision` accepte `refus_mode`, stocké dans `payload` jsonb (**sans migration**). Choix **consultatif** (n'écrit pas de statut métier — évite les CHECK sur 6 tables). Vérifié : logique 12/12 + parsing JS 8/8 + e2e cloud (créer/refuser/nettoyer) + capture modale.

## 2026-07-21 — Validation Direction : onglet « Traités » + rebouclage sur les listes d'origine
- [Frontend](01-frontend.md) — cockpit Direction : nouvel onglet **« Traités »** (`panelTraites`) = historique des validations décidées, filtrable par **catégorie** (pastilles domaine) + **décision** (validées/refusées). Et **badge « Direction ✓/✗/… »** ajouté sur chaque liste d'origine (avoir/NC/facture fournisseur/salarié/BC/objets HSE) via helpers `buildValDirMap`+`valDirBadge` (`shared.ts`). [Backend](02-backend.md) + [DB & API rest](03-db-api-rest.md) — **sans migration** : on LIT la table `validations` par `(ref_table, ref_id)` (les décisions y étaient déjà stockées) ; chaque route service charge `getValidations()`. Correctif : Sécurité envoyait `ref_table:'hse'` (inexistante) → vrai token d'entité. Décision **consultative** (affiche, n'annule pas l'objet). Avant : le décidé disparaissait et ne revenait jamais sur l'origine. Vérifié : build + test logique + smoke des 6 listes (2 bugs de portée corrigés) + harnais 59/0.

## 2026-07-21 — 🐳 Dockerisation LOCALE : app (Node) + Supabase auto-hébergé
- [Docker](04-docker.md) passe **🔜 → 🟡 (local construit)** : dossier `docker/` = `docker compose up` monte 8 conteneurs (compose Supabase officiel **curé** : cœur db+rest+kong+app, confort storage+studio ; retirés realtime/pooler/functions/gotrue). [Backend](02-backend.md) — app portée sur **Node** : `src/server.node.ts` (`@hono/node-server`) + `src/db.ts` lit `SUPABASE_URL`/`ANON_KEY` de l'env **avec repli cloud** → **Cloudflare inchangé**. [DB & API rest](03-db-api-rest.md) — base Docker **séparée** du cloud, démarre vide (schéma non versionné → `pg_dump`), volumes nommés `db-data`/`storage-data`. **Ne remplace PAS la prod** (Cloudflare + cloud intacts, CI push→déploie inchangé). Vérifié : app tourne réellement sous Node (GET / 200, static, login) + `docker compose config` valide + harnais 59/0 ; ⚠ `up` pas encore lancé (démon éteint). Doc : `docs/technique/12-docker-installation.md`.

## 2026-07-20 — Avoirs : n° par affaire (AV-affaire) ou libre (AVL-NNN) + charger une facture
- [Frontend](01-frontend.md) — form « Nouvel avoir » : champs **Rattacher à une affaire** (datalist) + **Charger une facture client** (remplit affaire+client, aperçu du n° live). [Backend](02-backend.md) — `POST /api/credits` : `num_affaire` → `AV-AAAA-<affaire>` (suffixe si multiple), sinon `AVL-AAAA-NNN`. `pageServiceCommercial` reçoit `getFacturesClient`. Vérifié e2e (AV-2026-TESTAFF9 / -2 / AVL-2026-001) — 59/0.

## 2026-07-20 — Correctif : « Émettre un avoir » créait juste une validation, pas l'avoir
- [Backend](02-backend.md) + [DB & API rest](03-db-api-rest.md) — nouvelle route **`POST /api/credits`** : le bouton *Émettre l'avoir* (Commercial) ne persistait RIEN (`submitSvcForm('avoirs')` = stub `submitValidation` `ref_id:null`) → l'avoir n'apparaissait jamais. Désormais l'avoir est **créé dans `credits`** (`AV-AAAA-NNN`, `statut='actif'`), puis soumis à la validation Direction avec son vrai id si la case est cochée. ⚠ `credits.client_id`=FK clients. Vérifié e2e + navigateur (build servi contient `fetch("/api/credits")`, stub disparu) — 59/0.

## 2026-07-20 — Fiche affaire enrichie (PV/opérateur/matière-BDT) + badges onglets partout + header linéarisé
- [Frontend](01-frontend.md) — `pageAffaireFiche` : sections **BDT+opérateur**, **PV de contrôle**, colonne **BDT consommateur** sur les matières ; **en-tête = `pageHeader` standard** (linéarisé) + retour. **Badges de comptage** ajoutés/complétés sur les onglets-listes de TOUS les services (Commercial + qualité/sécurité/prod/oas/compta/expéditions/plans/stock/maintenance) via `serviceHeader({tabs:[{…,badge}]})`.
- [Backend](02-backend.md) + [DB & API rest](03-db-api-rest.md) — `getCommandeDetail` étendu (`pvs` depuis `pv_controle`, `operateur_id/nom`+`lot_id` sur BDT, `bdt_id` sur matières) → propagé par `getAffaireDetail`. Résolution nom opérateur via `getOperateurs`. Vérifié e2e (opérateur « Lloyd Pierre », PV cpk 1.45, matière↔BDT) + navigateur — 59/0. ⚠ `mouvements_stock` = append-only (DELETE bloqué RLS anon).

## 2026-07-20 — Commercial : onglet « Affaires » + fiche affaire 360
- [Frontend](01-frontend.md) — `pageServiceCommercial` : onglet **`affaires`** (liste par `num_affaire`, mini-circuit DT/Offre/Commande) ; nouvelle page **`pageAffaireFiche`** (`/commercial/affaire/:num`) = Pilotage 360 + stepper circuit + sections DT/offre/commande/lots/BL/factures/NC/matières/avoirs.
- [Backend](02-backend.md) — **`getAffaireDetail(num)`** (queries.ts) agrège par `num_affaire` (table `affaires` vide → non utilisée) en réutilisant `getCommandeDetail`. Route `GET /commercial/affaire/:num`. Vérifié e2e + navigateur (affaire réelle `0001` listée ; fiche circuit) — 59/0.

## 2026-07-20 — Offre marge € · liste DT analysées · ADT ré-éditable jusqu'à acceptation
- [Frontend](01-frontend.md) — `pageOffreEdit` : ajout **Marge €/pièce**, **Marge € série** et **Marge totale (€)** (synthèse). `pageServiceBE` (onglet Analyse DT) : nouvelle table **« DT analysées — dans les offres »** (`dans_offres`) avec statut de l'offre + bouton **« Ré-éditer l'analyse »** (ou **« Analyse figée »** si offre acceptée).
- [Backend](02-backend.md) — `POST /api/dt/:id/analyse` : **garde-fou 409** si offre liée acceptée + **préservation** de `pieces_detail[].marge_pct` (ne réinitialise plus à 20 % — évite d'écraser le prix commercial). `/be/service` passe `getOffres()` à `pageServiceBE`. Vérifié e2e (marge préservée / 409) + navigateur (marge € live + gating BE) — 58/0.

## 2026-07-20 — Commercial : édition d'offre en page dédiée (façon analyse DT)
- [Frontend](01-frontend.md) — `pageOffreEdit` (`commercial.tsx`), page pleine `/commercial/offre/edit?id=OFF-…` : coûts par produit + **coefficient de marge (×) par produit** (PV live) + certifs/traçabilité (`qref.normeReferentiel`) + avoirs applicables du client + net. Le crayon d'offre y navigue (modale 3 champs retirée) ; verrou `ErpLock`.
- [Backend](02-backend.md) — `POST /api/offre/:id/pricing` : marge par produit persistée sur `demandes_travaux.pieces_detail[].marge_pct`+`prix_vente` (offre↔DT 1:1, **sans migration**), puis recalcul serveur `montant`/`montant_revient`/`marge` de l'offre (idempotent). `GET /commercial/offre/edit` charge offre+DT+avoirs. Vérifié e2e + navigateur (58/0).

## 2026-07-20 — UX (3/3) : listes rafraîchies sans perdre sa place (softReload)
- [Frontend](01-frontend.md) — nouveau `softReload()` (script du `layout`, shared.ts) : mémorise/restaure la **position de défilement** autour du rechargement (onglet conservé via le hash) → plus de saut en haut de page après un CRUD. Les **82 `location.reload()`** convertis (15 fichiers). Choix : rechargement *fluide* universel (contrainte « ne rien casser ») ; le zéro-reload par liste reste ouvrable à la carte. **Chantier UX en 3 briques TERMINÉ.**

## 2026-07-17 — UX (2/3) : sélecteurs de liste recherchables (combobox)
- [Frontend](01-frontend.md) — enhancer global (script du `layout`, shared.ts) : remplace le rendu natif de **tous** les `<input list=datalist>` par un combobox stylé (filtrage frappe, clavier), en préservant l'`onchange` (re-déclenche `change` à la sélection). Non invasif, aucun call-site touché. Reste : brique 3 (listes sans rechargement).

## 2026-07-17 — UX (1/3) : confirmations en modale interne
- [Frontend](01-frontend.md) — `appConfirm(msg)` (shared.ts, script global du layout) = **modale au design de l'ERP**, remplace `window.confirm` sur les **49** appels (13 fichiers). Fonctions appelantes passées `async`. Convention : `await appConfirm(...)`, plus de `confirm()` natif.
- Reste (demande user) : (2) sélecteurs recherchables, (3) listes sans rechargement (`location.reload` → refresh ciblé).

## 2026-07-17 — Plan / Bâtiment : repères « poste » (à la place des « machine ») + stats au survol
- [Frontend](01-frontend.md) — `plans.tsx` : catégorie de repère `machine` → **`poste`** ; **bulle au survol** (`posteTipHtml`) = process contenus, machines, taux €/h, OPEX €/an.
- [Backend](02-backend.md) — `/plans/service` calcule les stats par poste (`computePosteRates` + agrégation `machines`/`process_atelier` + OPEX théorique) et les passe à la page.

## 2026-07-17 — Audit sécurité/perf/qualité (workflow adversarial) + correctifs sûrs
- [Backend](02-backend.md) — **Sécurité** : XSS `/login?next=` (validation server-side), XSS fournisseur/machine (échappement), **bypass auth joker ILIKE** corrigé, **rate-limit login** (429). **Perf** : recomputes solde BDT en Promise.all, getLotDetail parallélisé. `escX` centralisé (shared.ts) + code mort retiré.
- [Front](01-frontend.md) — payload mort `CAPA_STUDY_BY_RES` supprimé, clone JSON inutile retiré du planning, `esc→escX` en attribut (direction).
- [Maintenance](06-maintenance.md) — ⚠ **déploiement** : roter/poser `JWT_SECRET`+`BOOTSTRAP_*` en env (secret committé). **Perf à traiter** (passe dédiée) : `getDashboardData` (33 tables `select('*')` → saturation), bornage listes chaudes, fusion Promise.all qualite/securite/direction.

## 2026-07-17 — Machine rattachée à PLUSIEURS process (multi) + synchro Machine ↔ Process
- [Frontend](01-frontend.md) — modale machine : select unique → **cases à cocher** (`mc_proc_list`) des process du poste (multi) ; `createMachineFn` synchronise (coché→rattache, décoché→détache). Édition de process (`openProcEdit`/`saveProcEdit`) et modale machine écrivent le **même** `process.machine_id` → toujours cohérents.
- [Backend](02-backend.md) — aucune route nouvelle ; `PATCH /api/production/process/:id {machine_id}` appelé en boucle (rattache/détache). Plusieurs process peuvent pointer la même machine (modèle N process → 1 machine).

## 2026-07-17 — Machine → poste → process + CRU au taux horaire machine du process
- [Frontend](01-frontend.md) — modale machine (`/production/service`) : après le poste, select **« Process rattaché »** (`mcFillProcess`, process du poste ou « nouveau ») ; `createMachineFn` rattache la machine au process (`PATCH process {machine_id}`). Gamme BE : `machine_taux_h = cout_h` au choix du process, `nomEtapeCoutMachine` retombe sur le `cout_h` live.
- [Backend](02-backend.md) — **`computePosteRates.machineRate`** renvoie le **`cout_h` de la machine** (+ incrément OPEX poste, ou override) au lieu du taux stocké sur l'étape (souvent 0) ; robuste même sans poste. `getBeRefs` expose `machines[].cout_h`. ⇒ coût machine du CRU réellement compté (ex. DT-2026-0001 : machineSerie 0 → 156,55 €).
- [DB & API REST](03-db-et-api-rest.md) — aucune migration ; rattachement via `PATCH /api/production/process/:id {machine_id, requiert_machine, poste_id}`.

## 2026-07-17 — Gamme par POSTE→process · planning tous postes · OAS machine · UX Production
- [Frontend](01-frontend.md) — **Gamme (nomenclature `be.tsx` + analyse DT `index.tsx`) réordonnée [Type]·[Poste]·[Process]**, process filtré par poste ; boutons création machine/process **retirés** du BE (→ Production). **Planning par poste** : tous les postes affichés, BDT dérivé du process, dépôt sur mauvais poste refusé. **Production** : bouton « Demande d'achat » en header (plus flottant), **corbeille** pour supprimer un process, badge/filtre **OAS** machines.
- [Backend](02-backend.md) — **Bug OAS machine** : routes `POST`/`PATCH /api/production/machine` rabattaient OAS→Seem → whitelist élargie. `getBeRefs` expose `postes[]` + `poste_id` process (`select('*')` immunisé). `/be/analyse` injecte `window.__BE_REFS__`. Étapes libres persistent `poste_id`/`process_id`/`process_nom`. Revue adversariale : échappement `deleteProc`/`deletePosteFn`/pushNotif corrigé.
- [DB & API REST](03-db-et-api-rest.md) — **Migration `scripts_import/machines_activite_oas.sql`** (PAT) : CHECK `machines_activite_check` élargi à `OAS` (sinon INSERT/UPDATE machine OAS rejeté en base). Postes/process OAS déjà OK.

## 2026-07-17 — Analyse DT : temps & coûts **par poste** (défilables par quantité) + coût matière/accessoires par réf
- [Frontend](01-frontend.md) — `/be/analyse` : nouveau panneau **« Temps & coûts par poste »** (`beRenderPostes`) sous la gamme, avec **flèches ‹ › précédent/suivant** (`bePosteNav`) pour défiler les quantités et voir temps + coût série **par poste**. Couverture matière/accessoires enrichie (`beRenderCoverage`) : colonnes **Prix unité** + **Coût série** + ligne de total.
- [Backend](02-backend.md) — **`etapeDecomp`** (nouvelle fonction `src/shared.ts`) = **source UNIQUE** de la décomposition coût/temps d'une étape, partagée par `computeNomCostForQty` (CRU) **et** l'endpoint `GET /api/be/analyse-dt/:id` ⇒ le total « par poste » **réconcilie au centime** avec le CRU. Piège corrigé : `machine_taux_h=0` ≠ repli défaut (`||50`) mais taux **0** + incrément OPEX poste. CRU inchangé (non-régression).
- [DB & API REST](03-db-et-api-rest.md) — aucune migration ; endpoint enrichi : `etapes[].{poste_id,poste_nom,st_forfait,st_unit}` + `fournitures_stock[].{besoin_unite,cout_unite,cout_serie}`. Poste résolu via `machine.poste_id` / `process.poste_id`.

## 2026-07-17 — Analyse DT : validation ouverte au BE · frais généraux /lot · marge → offre · synthèse
- [Frontend](01-frontend.md) — `/be/analyse` : **frais généraux PAR LOT** (amortis /qté), **marge retirée** (colonnes PV supprimées ; la marge se fixe dans l'offre), **synthèse = coût total DT** (Σ séries, plus de moyenne CRU/pc). Offre : PV = `CRU × (1 + marge de l'offre)`.
- [Backend](02-backend.md) — **RBAC** : `serviceFor('/api/dt/:id/analyse') = 'be'` (avant : `commercial`) → tout `bei` peut valider. Endpoint `POST /api/dt/:id/analyse` : CRU envoyé par le BE (fg /lot amorti), marge d'offre par défaut 20 %.
- [DB & API REST](03-db-et-api-rest.md) — aucune migration ; `pieces_detail` stocke `cout_fg` (par lot), `cru`, `quantites_chiffrage[{quantite,cru,cru_serie}]`.

## 2026-07-17 — Éditer un process depuis son poste (nom · taux horaire · machine)
- [Frontend](01-frontend.md) — bouton ✏️ sur chaque process listé sous un poste → modale (nom, machine, **taux horaire €/h**, poste). Taux horaire = `cout_h` de la machine ; l'éditer met à jour la machine (→ CRU). Manuel = champ désactivé.
- [Backend](02-backend.md) — `saveProcEdit` : `PATCH /api/production/process/:id` puis (si machine) `PATCH /api/production/machine/:id {cout_h}`.

## 2026-07-17 — Postes & Process gérés par poste (glisser-déposer) · coûts en unités d'achat entières
- [Frontend](01-frontend.md) — Production/Process Ateliers : **référentiel à plat retiré**. Chaque poste porte **« + Process »** (1er) et **« + Machine »** (poste pré-rempli). Process non rattachés = bac **« À rattacher »** en bas, **glisser-déposer** sur un poste. Analyse DT : besoin affiché en **tôles / paquets**.
- [Backend](02-backend.md) — `computeNomCostForQty` : matière en **tôles entières** `ceil(qté/nb_par_tole)×prix_tole`, accessoires en **paquets** `ceil(nb×qté/qte_paquet)×prix_paquet` (arrondi supérieur, généralisé). Rattachement process→poste = `PATCH /api/production/process/:id {poste_id}` (`attachProcToPoste`).
- [DB & API REST](03-db-et-api-rest.md) — aucune migration (jsonb/colonnes existantes) ; couverture `/api/be/analyse-dt` renvoie `besoin` (unités d'achat) + `besoin_unite`.

## 2026-07-16 — Phase E : achat machine → OPEX poste → taux effectif → CRU · analyse DT (série ×q + paquets)
- [Frontend](01-frontend.md) — Production/Postes : colonne **« Taux/h effectif »** (+ « dont +X achats ») + bouton override (`posteSetTaux`, confirmation). Analyse DT (`/be/analyse`) : tableau de résultats en **TOTAL SÉRIE ×q** (Matière/Access./MO+mach/S-trait/Frais g/CRU total) + CRU/pc + PV/pc. Maintenance/Finances affichent le même taux.
- [Backend](02-backend.md) — `computePosteRates` (`src/shared.ts`) : **taux poste = base + (Σ achats_ht / Σ capacité×220)** ou override ; alimente le **CRU** via `computeNomCostForQty(opts.machineRate)`. Réception BL d'un BC `categorie='machine'` → `upsertMachineOpexAchat` sur **`achats_ht`** (idempotent via CAS `opex_greffe`). **Accessoires en paquets indivisibles** (`ceil(nb×q/qte_paquet)×prix_paquet`). **Non-régression garantie** (incrément 0 sans achat).
- [DB & API REST](03-db-et-api-rest.md) — `machines_opex.achats_ht` (dédié, ≠ `cout_total_ht`), `postes.taux_horaire_manuel`, `bons_de_commande.opex_greffe/categorie/machine_id/poste_id`, **RLS permissive `machines_opex`** — migration `scripts_import/machines_achats_opex_schema.sql` (PAT, re-run idempotent).

## 2026-07-16 — Postes d'atelier (machines + process) · planning par poste · OPEX par poste
- [Frontend](01-frontend.md) — Production/onglet Machines : carte **« Postes de travail »** (CRUD) + menu « Poste » sur les modales machine/process. Planning : board **« Planning par poste »** (BDT placés au poste de leur process). Maintenance : onglet **« Coûts postes »** (TCO par poste).
- [Backend](02-backend.md) — **OPEX/coût PAR POSTE = somme des machines du poste** (conso réels + théorique) + conso tirées au poste ; **même calcul en Production et Maintenance**. Routes `/api/production/poste` (CRUD) ; `poste_id` accepté sur machine/process/sortie-matière/BDT ; `getPostes()` fail-soft.
- [DB & API REST](03-db-et-api-rest.md) — nouvelle table **`postes`** + colonnes `poste_id` (machines, process_atelier, mouvements_stock, bons_de_travail) via `scripts_import/postes_schema.sql` (DDL = PAT, à exécuter dans Supabase ; FK `ON DELETE SET NULL`).

## 2026-07-16 — Analyse DT : chiffrage auto verrouillé + multi-quantités · accessoires corrigés
- [Frontend](01-frontend.md) — `/be/analyse` : les coûts (matière, **accessoire** en ligne à part, MO+machine, sous-traitance) sont **calculés et verrouillés** ; **seuls frais généraux + marge** se saisissent. **Tableau de prix par quantité** (une ligne par quantité demandée, prix/pc décroissant). Bloc DT (Commercial) : champ **« Quantités à chiffrer »**. Nomenclature : sélecteur **accessoire** réparé (datalist de toutes les réfs, désignation + fournisseur auto par catalogue).
- [Backend](02-backend.md) — `GET /api/be/analyse-dt/:id` renvoie `results[]`/`quantites[]` (un chiffrage par quantité, réglage amorti) ; `POST /api/dt/:id/analyse` accepte `cout_accessoire` + `quantites_chiffrage` (montant/offre restent sur la quantité principale).
- [DB & API REST](03-db-et-api-rest.md) — `pieces_detail` (jsonb) porte désormais `quantites[]` et `quantites_chiffrage[]` par pièce (aucune migration DDL).

## 2026-07-16 — Confidentialité champ (taux horaire RH)
- [Backend](02-backend.md) — RBAC : ajout du principe de **confidentialité au niveau champ** (le taux horaire de la fiche employé n'est envoyé au client que si droits d'écriture RH ; masqué `***`, révélé au clic maintenu).

## 2026-07-16 — Refonte analyse DT + avoirs + stock↔catalogue
- [Frontend](01-frontend.md) : ajout de l'**analyse DT** riche (`/be/analyse`) — listes reliées à la base, gamme drag-drop (verrouillées + glissables), CRU live, upload GED, triplet réf/plan/client.
- [Backend](02-backend.md) — flux métier mis à jour : **BE** DT → analyse (déverse nomenclature + couverture stock) → offre ; **Commercial** avoirs affichés en net et **décomptés à l'acceptation** ; **Stock** toute nouvelle réf catalogue → entrée stock auto.
- [DB & API REST](03-db-et-api-rest.md) — pièges ajoutés : `credits` = avoirs (pas `avoirs`) ; `cahier_charges` = `text[]` (analyse dans un élément tagué) ; `documents.categorie` += `analyse_dt` ; **stock reflète le catalogue** (réf ≠ désignation) ; backfill 72 réfs catalogue → stock.

## 2026-07-13 — Création du cerveau
- Dossier `cerveau application/` créé : hub central ([`README.md`](README.md)) + **7 briques**.
- Briques ✅ Fait : [Frontend](01-frontend.md), [Backend](02-backend.md), [DB & API REST](03-db-et-api-rest.md), [Maintenance](06-maintenance.md), [Utilisation](07-utilisation.md).
- Briques 🔜 À venir (cible décrite, à réécrire au réel) : [Docker](04-docker.md), [N8N](05-n8n.md).
- Ancré sur le code réel (35 fichiers `src/`, ~290 routes, 28 schémas SQL versionnés, 6 skills).
