> 🧠 [Cerveau (accueil)](README.md) · Voisines : [⚙️ Backend](02-backend.md) · [📖 Utilisation](07-utilisation.md)

# 🖥️ Brique 01 — FRONTEND

**État : ✅ Fait** — c'est l'interface que voit l'utilisateur dans son navigateur.

## L'idée en une phrase
Le frontend est **rendu côté serveur** (SSR) : le serveur fabrique la page HTML **entièrement finie** et l'envoie au navigateur. **Aucun framework client** (pas de React/Vue). L'interactivité est du **JavaScript vanilla** glissé dans les pages.

Pourquoi ce choix ? Simplicité et robustesse : une techno de moins à maintenir, un rendu rapide, et un mainteneur externe n'a besoin de connaître que **HTML + JS de base**.

## Comment c'est fait ici
- **TSX côté serveur** : chaque page est une fonction qui renvoie du HTML (via JSX Hono). Ex. `pageDTStatuts(...)`, `pageClientFiche(...)`.
- **Layout commun** : [`src/shared.ts`](../src/shared.ts) (621 l.) = la barre latérale, l'en-tête, les helpers d'affichage réutilisés partout. [`src/renderer.tsx`](../src/renderer.tsx) = l'enveloppe HTML.
- **Style** : CSS **en ligne** dans les templates (pas de framework CSS). Cohérence assurée par les helpers de `shared.ts`.
- **Interactivité** : du JS écrit dans des *template strings* (ex. `onclick="nomRefClientAdd()"`). Les gros écrans (planning, dashboards) embarquent leurs propres fonctions.
- **Graphiques** : [`src/dash_ui.ts`](../src/dash_ui.ts) + Chart.js pour les 14 tableaux de bord.
- **Formulaires riches** : certains écrans embarquent beaucoup de JS client — ex. l'**analyse DT** ([`/be/analyse`](../src/index.tsx), dans le routeur) : listes déroulantes reliées à la base, gamme drag-drop (étapes verrouillées + glissables), CRU recalculé en direct, **upload GED** de documents, triplet réf/plan/client. Même piège d'échappement que les autres pages (ci-dessous).
- **Connexion** : [`src/login.tsx`](../src/login.tsx) = l'écran matricule + PIN.

## Les pages (fichiers `.tsx`, par taille)
| Fichier | Rôle | Lignes |
|---|---|---|
| `commercial.tsx` | Clients, offres, commandes, DT, fiche client | 3 409 |
| `prod.tsx` | Production, OF/BDT, lots, machines | 3 287 |
| `be.tsx` | Bureau d'études : nomenclatures, GED, répertoire réfs | 2 557 |
| `qualite.tsx` | Non-conformités, 8D, contrôles, dérogations | 2 137 |
| `planning_bdt_bds.tsx` | Le planning de production (drag, encadrés) | 1 794 |
| `rh_service.tsx`, `affectation.tsx`, `compta_service.tsx`, `listes.tsx` | RH, affectation, compta, listes/référentiels | 1 200-1 500 |
| `stock_service.tsx`, `securite.tsx`, `achats.tsx`, `expeditions.tsx`, `oas.tsx` | Stock, HSE, achats, expéditions, traitement OAS | ~1 000 |
| + `maintenance_service.tsx`, `finances.tsx`, `rapport8d.tsx`, `dashboards.tsx`, `fournisseur_fiche.tsx`, `direction_service.tsx`, `plans.tsx` | Autres services | 400-855 |

## Ajouts récents utiles à connaître
- **`appMotif`** (`src/shared.ts`) : fenêtre de saisie d'un **motif obligatoire** (Ctrl+Entrée valide, Échap annule), sœur d'`appConfirm`. Utilisée pour la suppression d'une nomenclature validée (motif inscrit au journal EN 9100) — y compris en **suppression groupée** (le moteur `bulkSelectAssets` accepte `motif: true` et vérifie la saisie **avant** de fermer la fenêtre).
- **Qualité › Quarantaine** : un lot reçu d'un fournisseur ouvre « Décider » (fenêtre `quarFournModal`) au lieu de « Statuer » — renvoi, dérogation fournisseur ou entrée partielle, montant d'avoir pré-rempli au prix unitaire du BC.
- **Achats › Avoirs fournisseurs** (7ᵉ onglet) et **Expéditions › Envois › Retours fournisseurs** : les deux bouts visibles du même flux.
- **Coût d'une étape dans le navigateur = `BE_ETAPE_COUT_JS`** (`src/be.tsx`, 14/09/2026) : une chaîne JS brute (ni accent grave, ni `${`, ni barre oblique inverse) qui recopie **exactement** le moteur serveur `etapeDecomp` / `construireTauxAtelier` ; injectée telle quelle dans le formulaire nomenclature **et** dans `/be/analyse`. Toute évolution de la règle de coût se fait **aux deux endroits** (`src/shared.ts` + cette chaîne) — un test de parité l'a vérifiée (11 697 comparaisons). Seules des **moyennes** du coût chargé RH partent au navigateur, jamais un taux individuel.
- **Production › planning (lot C, 14/09/2026)** : chemin critique repris ligne à ligne de `src/gamme.ts` dans le script de la page (marqueurs `==CC-MOTEUR-DEBUT==` / `==CC-MOTEUR-FIN==`, test de parité) ; réglage | réalisation sur cartes, barres et fenêtres ; découpe (seule la réalisation) et « Annuler la découpe » depuis la goulotte ; Présence à 4 créneaux par menu, écritures en file par case, semaines non lues verrouillées.
- **Expéditions › réception et PV (lot D, 14/09/2026)** : fenêtre de réception (`#exp-recep-overlay`, deux modes : réception / « Corriger les informations ») et fenêtre de PV (`#exp-pv-overlay`, tableau des lignes du BC construit **en texte** dans le DOM, radios `.pv-l-rep`, « Tout à Oui ») ; boutons `.exp-pv-open[data-bc][data-bl]` et `.exp-rec-edit[data-bl]` par **délégation d'événements** ; données `EXP_BC` (contenu commercial vidé sans écriture Expéditions), `EXP_PV` (contrôleurs, droits), `EXP_BLREC`. La validation de la page **recopie** celle du serveur (`src/reception.ts`, `src/pv_reception.ts`) : champ fautif en rouge à partir du `champ` / `idx` renvoyé. **Achats** : case « Certificat matière requis » (`bc_certificat`, `bcw_certificat`) et bouton `.ach-bc-cert[data-id]`.
- **Stock › Rangement / Mise en stock (lot F, 15/09/2026)** : premier onglet (`panelMiseEnStock`, `src/stock_service.tsx`) avec sous-onglets À ranger / Ajustement de stock / Types & zones ; « Gestion du stock » vient en 2ᵉ (16/09/2026 : `TABS` serveur et `STK_TABS` client à garder dans le même ordre) ; onglet et sous-onglet écrits dans le hash (`#mes`, `#mes-types`, `#gestion-catalogue`…) et restaurés par une IIFE ; fenêtre « Accepter l'entrée en stock » construite en JS (`stkOuvrirRangement`, cadre d'emplacement redessiné **seulement** quand le type ou l'emplacement fixe change) ; données `STK_FILE`, `STK_TYPES`, `STK_ZONES`, `STK_ARTS` en `sjX` (projection complète de ce que lit le JS) ; délégation `data-act` / `data-act-change` ; `stkEsc` sur tout texte de la base ; boutons d'écriture grisés pour un lecteur (`peutEcrire`) ; « Masquer les produits vides » mémorisé en `localStorage` (try/catch) ; panne ≠ vide (« File illisible », badge « ? »). Niveaux d'approvisionnement : type et emplacement éditables en ligne (`appMotif`), historique en fenêtre. **PV de réception** (`src/expeditions.tsx`) : tableau des quantités en Conforme comme en Non conforme (`expPvPrevues` = copie exacte de `qtesPrevuesPV`, `qte_prevue_affichee` renvoyée), « Conforme ? » recalculé à la frappe (`input` délégué), case Reliquat, résumé des effets ; case « Livraison partielle » retirée de la réception. **Qualité** : bloc « Part acceptée par ligne » dans `quarFournModal`. **Direction** : boutons Accepter / Refuser et fenêtre de refus sans annulation pour `excedent_reception`. **Achats** : badge « Reliquat de BC-… · date à valider ». **`notifDurable`** (`src/shared.ts`) : notification réaffichée après `softReload` (même page, 30 s) — pour les avertissements qui sont parfois la seule trace d'un effet raté.
- **Production › bandeau (15/09/2026)** : `blocDaNcProduction` (`src/prod_da_nc.ts`, HTML + JS client **hors** de `prod.tsx`) pose « Demande d'achat » et « PV de non-conformité » dans `#svc-hdr-bar` ; fenêtres `#odaModal` / `#pvncModal` rangées **hors des panneaux** (ouvrables depuis tout onglet — cf. le piège de la modale sous un onglet caché) ; formulaire complet dès le clic, identification vérifiée à l'envoi ; le serveur renvoie `champ` et le JS surligne `<prefixe>_<champ>` ; `pfEsc` avant tout `pushNotif` (qui insère en HTML) ; `PRODFORM_REF` en `sjX`. Fenêtre de soldage : « Reçu par », `#s_solder_btn` désactivé pendant l'envoi.
- **Production › cadence usine et planning en heures ouvrées (lot G, 16/09/2026)** : `scriptCadencePage(lecture)` (`src/cadence_ui.ts`) injecte **une** balise avant les panneaux : `CADENCE_PAGE` (disponible / absente / erreur), moteur **`CAD`** (copie ES5 de `src/cadence.ts`, `==CAD-MOTEUR==`), `cadCalendrier`, `cadHorairesCreneau`, `cadNotifierChangement` (redessine présence, affectation, Gantt, volet). 3ᵉ volet « Cadence usine » (`volShow('cad')`, `?tab=machines&vol=cad`, délégation `data-cad-action`, saisies non enregistrées conservées au redessin). Planning : horloge `ccHorloge` (copie de `tempsPlanning`), blocs copiés `==POSTE-OAS==` (G3 chevauchement, G4 remise en goulotte `ppPlanRemiseGoulotte` + `ppConfirmerRemise`) et `==PLAN-CADENCE==` (heure fermée, fenêtre du jour) ; `PLAN_AXE` / `planMajAxe` : l'axe suit la plage ouverte du jour (DAY_START / DAY_END variables, CC_DEBUT / CC_FIN = grille), hachures par ligne selon le site du poste, dépôt borné, icônes `data-deborde` (lune / pause / flèche), badges « → OAS » et « Remis en goulotte », tri `pendSort` par `remis_goulotte_le`. **Quatre copies client** à garder identiques au serveur (tests de parité du scratchpad).
- **Mères et sous-lots (lot H2, 18/09/2026)** : BE — bloc « Composants — ordre de fabrication » en tête de colonne droite (poignée de glisser-déposer, rang, ▲ ▼, flèches, n° de sous-lot, recherche à deux groupes `NOM_COMPOSABLES`, exclusions `NOM_ANCETRES` calculées **au rendu serveur**, arborescence dépliable, résumé de fabrication, 409 cycle / profondeur affichés sans fermer le formulaire), bouton libellé **« Demande de prix »** (`.nom-rfq-btn`, 3 états), journal EN 9100 **pleine largeur**. Production — listes de lots **en arbre** (`trierArborescence`), fiche commande en **ordre de fabrication** (`trierOrdreFabrication`), fiche lot (9ᵉ paramètre `arbre` de `pageLotDetail` : sous-lots, avancement de l'arbre, vigilance, bouton « Créer les sous-lots »), OF « Sous-ensembles à assembler », goulotte triée par `ORDRE_FAB` (`cmpOrdreFab`), pastille « Sous-lot 01.02 », badge « Sous-lots en cours » (`VIGIL_PAR_ID`). Le module `src/nomenclature_arbre.ts` n'est **jamais** injecté au navigateur (deux miroirs de trois lignes, commentés).
- **Production › Process Ateliers** : le **taux horaire machine** se saisit sur le process (modales `procModal` / `procEditModal`, type choisi explicitement) ; machines et postes n'ont plus de taux. Un avertissement serveur perdu par le rechargement est gardé en `sessionStorage` puis réaffiché.

## ⚠️ Le piège central du frontend (à connaître absolument)
Le JS client vit **dans des chaînes de texte** TSX. Les apostrophes et retours à la ligne doivent être **doublement échappés** (`\\'`, `\\n`), sinon la page casse silencieusement. **C'est la 1ʳᵉ cause de régression du dépôt.**
→ Filet de sécurité : `node scripts_doc/harness_all_pages.mjs` (le *harnais*) exécute le JS de **toutes** les pages et attrape ces bugs. Conventions détaillées dans le skill [`erp-new-module`](../.claude/skills/erp-new-module/SKILL.md).

## Quand cette brique change → à maintenir
1. **L'UI a changé** → rafraîchir les captures : skill [`erp-screenshots`](../.claude/skills/erp-screenshots/SKILL.md) + mettre à jour [📖 Utilisation](07-utilisation.md).
2. **Toujours** → harnais vert avant commit.
3. Mettre à jour cette page si un écran majeur est ajouté/retiré.

## Pour aller plus loin
[`docs/technique/01-architecture.md`](../docs/technique/01-architecture.md) · [`docs/technique/05-conventions-code.md`](../docs/technique/05-conventions-code.md)
