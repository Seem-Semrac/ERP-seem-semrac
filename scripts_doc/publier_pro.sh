#!/usr/bin/env bash
# ===========================================================================
#  OBSOLETE depuis le 9 septembre 2026.
#  Seem-Semrac/ERP-seem-semrac est desormais le depot PRINCIPAL (origin) :
#  on y committe directement, il n'y a plus d'etape de publication separee.
#      git add -A && git commit -m "..." && git push
#  Puis, sur la VM :  ~/erp/docker/scripts/erp-docker.sh maj
# ===========================================================================
echo
echo "  Ce script n'est plus necessaire."
echo
echo "  Seem-Semrac/ERP-seem-semrac est maintenant le depot principal (origin)."
echo "  On y committe directement :"
echo
echo "      git add -A && git commit -m \"ce que contient la mise a jour\" && git push"
echo
echo "  Puis, sur la VM :"
echo
echo "      ~/erp/docker/scripts/erp-docker.sh maj"
echo
